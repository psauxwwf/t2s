# Changes

- Added a `freeturn` backend that runs the upstream free-turn-proxy Go client in-process on the generated relay port, validates its config, registers it with t2s routing, and documents usage/limitations.
